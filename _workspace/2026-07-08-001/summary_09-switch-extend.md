# humanize summary — 09-switch-extend.md

- 변경률: 약 5% (prose 기준. 원문이 이미 건조한 기술 레퍼런스 register 를 유지하고 있어 수정 소요가 적었음)
- 등급: B (S1 잔존 0, S2 잔존 ≤2, 자체검증 6/6 통과 — A 기준의 변경률 하한 10% 미달로 B 표기)
- 자체검증: 6/6 통과 (고유명사·수치 보존 / 변경률 / 장르 / register / S1 잔존 0 / 인공 표현 미추가)
- 탐지 카테고리:
  - C-11 (S1) 연결어미 뒤 쉼표 — `이지만,` `싸고,` `하고,` `이고,`×3 총 6곳 쉼표 제거
  - A-18 (S2) 좌향 장수식 — 매니저 교체 절 1곳 문장 분리
- 스코프 준수: frontmatter · mermaid 펜스 3개 · 표 1개 · 헤딩 · 기술 용어(onchain-events, sweep, vault, RBF, EIP-7702 등) byte-identical

## 대표 수정

1. C-11: "지금 매니저 내부 연동은 Fireblocks 이지만, 두 백엔드는" → "Fireblocks 이지만 두 백엔드는"
2. A-18: "벤더 교체는 **같은 API 계약을 지키고 같은 토픽(onchain-events)에 같은 이벤트 스키마를 publish 하는 다른 매니저 구현으로 바꾸는 배포 수준**의 일입니다." → "벤더 교체는 **배포 수준**의 일입니다 — 같은 API 계약을 지키고 같은 토픽(onchain-events)에 같은 이벤트 스키마를 publish 하는 다른 매니저 구현으로 바꾸면 됩니다."
3. C-11: "서명·키·노드·전파는 **벤더 안**이고, 이쪽엔" → "**벤더 안**이고 이쪽엔" (동일 패턴 인프라 절 2곳)

## 비수정 판단 (과윤문 가드)

- 합니다체(교체·코드 구조·인프라 절)와 평서체(요약·타임라인 캡션) 혼재는 절 단위로 일관 — E-7 은 대화·구어 한정이라 미적용, register 보존 우선
- "유지하다가," "않으므로," 는 C-11 목록 외 어미라 유지
- em-dash 다용은 이 문서군 고유 스타일이라 유지
